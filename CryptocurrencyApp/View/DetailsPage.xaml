<Page x:Class="CryptocurrencyApp.View.DetailsPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="HomePage" Height="600" Width="600"
         Background="#292929">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <ui:TitleBar Title="CryptocurrencyApp" FontWeight="Bold"/>
        
        <StackPanel Margin="0,40,0,0">
            <TextBlock Text="Name:" FontWeight="Bold" FontSize="16"/>
            <TextBlock Name="Name" Text="{Binding CryptoDataDetail.Name}" FontSize="14" Margin="0,0,0,10"/>

            <TextBlock Text="Supply:" FontWeight="Bold" FontSize="16"/>
            <TextBlock Name="Supply" Text="{Binding CryptoDataDetail.Supply}" FontSize="14" Margin="0,0,0,10"/>

            <TextBlock Text="Max Supply:" FontWeight="Bold" FontSize="16"/>
            <TextBlock Name="MaxSupply" Text="{Binding CryptoDataDetail.MaxSupply}" FontSize="14" Margin="0,0,0,10"/>

            <TextBlock Text="Market Cap (USD):" FontWeight="Bold" FontSize="16"/>
            <TextBlock Name="MarketCapUsd" Text="{Binding CryptoDataDetail.MarketCapUsd}" FontSize="14" Margin="0,0,0,10"/>

            <TextBlock Text="Volume (24 Hr USD):" FontWeight="Bold" FontSize="16"/>
            <TextBlock Name="VolumeUsd24Hr" Text="{Binding CryptoDataDetail.VolumeUsd24Hr}" FontSize="14" Margin="0,0,0,10"/>

            <TextBlock Text="Price (USD):" FontWeight="Bold" FontSize="16"/>
            <TextBlock Name="PriceUsd" Text="{Binding CryptoDataDetail.PriceUsd}" FontSize="14" Margin="0,0,0,10"/>

            <TextBlock Text="Change Percent (24 Hr):" FontWeight="Bold" FontSize="16"/>
            <TextBlock Name="ChangePercent24Hr" Text="{Binding CryptoDataDetail.ChangePercent24Hr}" FontSize="14" Margin="0,0,0,10"/>

            <TextBlock Text="VWAP (24 Hr):" FontWeight="Bold" FontSize="16"/>
            <TextBlock Name="Vwap24Hr" Text="{Binding CryptoDataDetail.Vwap24Hr}" FontSize="14" Margin="0,0,0,10"/>
        </StackPanel>

    </Grid>
</Page>
