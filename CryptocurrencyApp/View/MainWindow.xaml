<Window x:Class="CryptocurrencyApp.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CryptocurrencyApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0">
            <Button Name="btnRefresh" Content="{Binding IsLoading, Converter={StaticResource BoolToRefreshTextConverter}}" Command="{Binding RefreshDataCommand}" />
        </DockPanel>
        <DataGrid
            Grid.Row="1"
            ItemsSource="{Binding CryptoCurrency}"
            IsReadOnly="True"
            AutoGenerateColumns="False"
            SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Rank}" Header="Rank"/>
                <DataGridTextColumn Binding="{Binding Symbol}" Header="Symbol"/>
                <DataGridTextColumn Binding="{Binding Name}" Header="Name"/>
                <DataGridTextColumn Binding="{Binding PriceUsd}" Header="PriceUsd"/>
                <DataGridTemplateColumn Header="Datails">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Open"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!--<Grid Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" Background="White" Grid.Row="0"
              Grid.RowSpan="2">
            <TextBlock Text="Refreshing...." VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Grid>-->
    </Grid>
</Window>
