<Window x:Class="CurrencyAppTest.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CurrencyAppTest"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <ItemsControl ItemsSource="{Binding CryptoCurrency}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="20"/>
                        <TextBlock Text="{Binding Symbol}" FontSize="16"/>
                        <TextBlock Text="{Binding PriceUsd}" FontSize="16"/>
                        <TextBlock Text="{Binding MarketCapUsd}" FontSize="16"/>
                        <TextBlock Text="{Binding VolumeUsd24Hr}" FontSize="16"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
